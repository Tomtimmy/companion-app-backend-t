// jest.setup.js
import '@testing-library/jest-dom';

// We can add other global mocks or setup here if needed.
// For example:
// jest.mock('react-native-vector-icons');
// jest.mock('react-native-gesture-handler', () => require('react-native-gesture-handler/jestSetupFile.js'));

// If our component tests rely on specific environment variables, i can set them here.
// process.env.NODE_ENV = 'test';